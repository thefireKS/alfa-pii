# Local secrets for docker-compose. Copy to .env and set real values.
# The demo consumer secret is referenced by config/consumers.local.json via
# secret_env=PII_CONSUMER_DEMO_SECRET. It is passed to the container at runtime
# and never baked into the image.
PII_CONSUMER_DEMO_SECRET=demo-secret